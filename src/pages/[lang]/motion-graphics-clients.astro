---
import VideoLayout from "../../layouts/VideoLayout.astro";
import { clients } from "../../data/videos_client.js";

export function getStaticPaths() {
    return [{ params: { lang: "de" } }, { params: { lang: "en" } }];
}

const firstVideo = clients[0];
const { lang } = Astro.params;

const base = "https://arnemuench.de";
const canonical = `${base}/${lang}/motion-graphics-clients/`;
const en = `${base}/en/motion-graphics-clients/`;
const de = `${base}/de/motion-graphics-clients/`;
---

<VideoLayout
    video={firstVideo}
    type="client"
    list={clients}
    lang={lang}
    canonical={canonical}
    enUrl={en}
    deUrl={de}
/>
